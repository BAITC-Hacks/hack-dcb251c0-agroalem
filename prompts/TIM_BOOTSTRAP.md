# Tim bootstrap message

Read `prompts/TIM_MASTER_PROMPT.md` and follow it as the controlling instruction for this repository.

Start with Phase 0 only.
Do not rewrite the project before the audit.

Use branch `tim/backend`.

After each completed target action:
- run evidence;
- update `.codex/TIM_STATE.md`;
- commit atomically;
- push.

Never invent facts, endpoints, metrics, scenarios, or test outcomes.
