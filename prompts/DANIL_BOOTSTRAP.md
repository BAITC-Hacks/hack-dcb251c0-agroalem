# Danil bootstrap message

Read `prompts/DANIL_MASTER_PROMPT.md` and follow it as the controlling instruction for this repository.

Start with Phase 0 only.
Do not rewrite the project before the audit.

Use branch `danil/frontend`.

After each completed target action:
- run evidence;
- update `.codex/DANIL_STATE.md`;
- commit atomically;
- push.

Never invent backend endpoints, payloads, latency, scenario decisions, or successful states.
